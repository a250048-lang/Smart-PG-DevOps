---
- hosts: localhost
  connection: local

  tasks:

    - name: Remove old container
      shell: docker rm -f smartpg-container || true

    - name: Run new container
      shell: docker run -d --name smartpg-container -p 8000:8000 smartpg